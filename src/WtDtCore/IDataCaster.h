/*!
 * \file IDataCaster.h
 * \project	WonderTrader
 *
 * \author Wesley
 * \date 2020/03/30
 * 
 * \brief 数据广播器接口定义
 * 
 * 文件设计逻辑与作用总结：
 * 本文件定义了IDataCaster（数据广播器）接口，是WonderTrader框架中
 * 数据分发机制的核心抽象。该接口采用了策略模式（Strategy Pattern），
 * 定义了统一的数据广播行为规范。
 * 
 * 核心设计理念：
 * 
 * 1. 接口抽象（Interface Abstraction）：
 *    - 定义纯虚函数，不包含任何实现
 *    - 通过继承实现多态，支持不同的数据分发策略
 *    - 解耦数据生产者和数据分发方式
 * 
 * 2. 多种数据类型支持：
 *    - WTSTickData：Tick行情数据（最常用）
 *    - WTSOrdQueData：委托队列数据（Level-2行情）
 *    - WTSOrdDtlData：逐笔委托数据（Level-2行情）
 *    - WTSTransData：逐笔成交数据（Level-2行情）
 * 
 * 3. 策略模式实现：
 *    - 定义统一的broadcast接口
 *    - 具体广播策略由子类实现
 *    - DataManager持有IDataCaster指针集合
 *    - 运行时动态选择广播策略
 * 
 * 架构设计：
 * 
 *                  +------------------+
 *                  |  IDataCaster    |  <-- 抽象接口
 *                  +------------------+
 *                  | + broadcast()   |
 *                  +------------------+
 *                          △
 *                          |
 *          +---------------+---------------+
 *          |                               |
 *   +-------------+                 +--------------+
 *   | UDPCaster  |                 | ShmCaster   |  <-- 具体实现
 *   +-------------+                 +--------------+
 *   | UDP广播     |                 | 共享内存广播 |
 *   +-------------+                 +--------------+
 * 
 * 典型使用场景：
 * 
 * 1. UDP网络广播（UDPCaster）：
 *    - 将行情数据通过UDP协议广播到网络
 *    - 支持单播、组播、广播三种模式
 *    - 适用于局域网内的数据分发
 *    - 延迟较低（微秒级到毫秒级）
 * 
 * 2. 共享内存广播（ShmCaster）：
 *    - 将数据写入共享内存供其他进程读取
 *    - 进程间通信效率最高
 *    - 适用于本地多进程架构
 *    - 延迟极低（纳秒级）
 * 
 * 3. 自定义广播器：
 *    - WebSocket广播（实时Web推送）
 *    - Redis发布订阅（分布式场景）
 *    - 消息队列（RabbitMQ、Kafka等）
 *    - 日志记录器（将数据记录到文件）
 * 
 * 数据流转过程：
 * 
 *   Parser -> DataManager -> IDataCaster(s) -> 订阅者
 *             (数据管理器)   (广播器集合)     (策略/监控/分析)
 * 
 * 设计模式应用：
 * - 策略模式（Strategy）：定义一系列算法，让它们可以互相替换
 * - 观察者模式（Observer）：数据变化时通知多个订阅者
 * - 适配器模式（Adapter）：适配不同的数据分发机制
 * 
 * 扩展性考虑：
 * - 易于添加新的广播方式（继承IDataCaster）
 * - 支持多个广播器同时工作（组合模式）
 * - 接口稳定，实现灵活可变
 * - 零成本抽象（虚函数开销可忽略）
 */

#pragma once                                    // 防止头文件重复包含（现代C++推荐方式）

#include "../Includes/WTSMarcos.h"              // 包含WonderTrader宏定义（如命名空间宏）

// 前向声明（Forward Declaration）
// 使用前向声明而不是直接包含头文件的优点：
// 1. 减少编译依赖，加快编译速度
// 2. 降低头文件间的耦合度
// 3. 避免循环依赖问题
// 4. 接口中只需要声明指针/引用，不需要完整类定义
NS_WTP_BEGIN                                    // 开始WonderTrader命名空间
class WTSTickData;                              // Tick行情数据类
class WTSVariant;                               // 配置参数类（虽然声明了但未使用）
class WTSOrdDtlData;                            // 逐笔委托数据类
class WTSOrdQueData;                            // 委托队列数据类
class WTSTransData;                             // 逐笔成交数据类
NS_WTP_END                                      // 结束WonderTrader命名空间

/**
 * @class IDataCaster
 * @brief 数据广播器抽象接口
 * 
 * 该接口定义了数据广播的标准行为规范。所有需要实现数据分发功能的类
 * 都应该继承此接口并实现其虚函数。
 * 
 * 接口设计原则：
 * 1. 单一职责：只负责数据广播，不涉及数据处理
 * 2. 开闭原则：对扩展开放，对修改关闭
 * 3. 依赖倒置：高层模块依赖抽象接口，不依赖具体实现
 * 4. 接口隔离：接口简洁，只包含必要的方法
 * 
 * 已知实现类：
 * - UDPCaster：UDP网络广播器
 * - ShmCaster：共享内存广播器
 * 
 * 线程安全性：
 * - 接口本身不保证线程安全
 * - 具体实现类需要自行处理线程同步问题
 * - 通常在DataManager的单一线程中调用
 * 
 * 性能考虑：
 * - broadcast方法应该尽可能快速返回
 * - 避免在broadcast中执行耗时操作
 * - 建议使用异步方式进行实际的数据发送
 * - 可以使用队列缓冲机制提高吞吐量
 * 
 * 使用示例：
 * @code
 *   // 创建具体的广播器
 *   IDataCaster* udp_caster = new UDPCaster();
 *   IDataCaster* shm_caster = new ShmCaster();
 *   
 *   // 注册到DataManager
 *   dataManager->add_caster(udp_caster);
 *   dataManager->add_caster(shm_caster);
 *   
 *   // DataManager会自动调用所有广播器的broadcast方法
 *   // 当收到tick数据时：
 *   for (IDataCaster* caster : casters) {
 *       caster->broadcast(tick);  // 多态调用
 *   }
 * @endcode
 */
class IDataCaster
{
public:
	/**
	 * @brief 广播Tick行情数据（纯虚函数，必须实现）
	 * 
	 * 该方法用于广播实时Tick行情数据，是最常用的广播接口。
	 * 子类必须实现此方法以定义具体的广播行为。
	 * 
	 * Tick数据包含：
	 * - 基本信息：合约代码、交易所、时间戳
	 * - 价格数据：最新价、开高低收、涨跌停价
	 * - 数量数据：成交量、持仓量、成交额
	 * - 盘口数据：五档或十档买卖盘
	 * 
	 * 实现建议：
	 * 1. 快速返回，避免阻塞主流程
	 * 2. 使用队列缓冲，异步发送
	 * 3. 处理好数据的生命周期管理
	 * 4. 考虑数据压缩以减少带宽
	 * 5. 添加错误处理和重试机制
	 * 
	 * 调用频率：
	 * - 高频场景：每秒数千次甚至更高
	 * - 低频场景：每秒几次到几十次
	 * - 必须保证高性能，避免成为瓶颈
	 * 
	 * 数据所有权：
	 * - curTick指针由调用者管理生命周期
	 * - 实现者应该复制需要的数据或增加引用计数
	 * - 不要假设指针在函数返回后仍然有效
	 * 
	 * @param curTick 当前Tick行情数据的指针
	 *                不能为NULL（调用者负责检查）
	 * 
	 * @note 纯虚函数，子类必须实现
	 * @note 实现时应该考虑线程安全性
	 * 
	 * @see WTSTickData Tick数据结构定义
	 */
	virtual void	broadcast(WTSTickData* curTick) = 0;
	
	/**
	 * @brief 广播委托队列数据（有默认实现的虚函数）
	 * 
	 * 该方法用于广播Level-2行情中的委托队列数据。委托队列显示了
	 * 最优买卖价位上的前N笔委托明细（通常是50笔）。
	 * 
	 * 委托队列数据包含：
	 * - 合约代码、交易所
	 * - 委托价格（买一或卖一价格）
	 * - 委托方向（买或卖）
	 * - 委托队列：50笔委托的数量数组
	 * 
	 * 默认实现：
	 * - 提供了空实现 {}
	 * - 如果子类不需要处理委托队列，可以不重写
	 * - 这符合接口隔离原则（ISP）
	 * 
	 * 使用场景：
	 * - Level-2深度行情分析
	 * - 大单追踪
	 * - 流动性分析
	 * - 高频交易策略
	 * 
	 * 性能考虑：
	 * - 委托队列数据量较大（50笔 * 每笔数量）
	 * - 更新频率可能很高
	 * - 建议使用二进制序列化减少开销
	 * 
	 * @param curOrdQue 当前委托队列数据的指针
	 * 
	 * @note 虚函数，子类可选择性重写
	 * @note 默认实现为空，不做任何处理
	 * 
	 * @see WTSOrdQueData 委托队列数据结构定义
	 */
	virtual void	broadcast(WTSOrdQueData* curOrdQue){}
	
	/**
	 * @brief 广播逐笔委托数据（有默认实现的虚函数）
	 * 
	 * 该方法用于广播Level-2行情中的逐笔委托数据。逐笔委托记录了
	 * 每一笔委托单的详细信息。
	 * 
	 * 逐笔委托数据包含：
	 * - 合约代码、交易所
	 * - 委托时间（微秒级精度）
	 * - 委托价格
	 * - 委托数量
	 * - 委托方向（买或卖）
	 * - 委托序号
	 * 
	 * 默认实现：
	 * - 提供了空实现 {}
	 * - 子类根据需要选择性重写
	 * 
	 * 使用场景：
	 * - 逐笔委托流分析
	 * - 市场微观结构研究
	 * - 高频交易信号生成
	 * - 算法交易订单流分析
	 * 
	 * 数据特点：
	 * - 数据量非常大，每秒可能数千笔
	 * - 实时性要求高
	 * - 需要高效的序列化和传输机制
	 * 
	 * @param curOrdDtl 当前逐笔委托数据的指针
	 * 
	 * @note 虚函数，子类可选择性重写
	 * @note 默认实现为空，不做任何处理
	 * 
	 * @see WTSOrdDtlData 逐笔委托数据结构定义
	 */
	virtual void	broadcast(WTSOrdDtlData* curOrdDtl){}
	
	/**
	 * @brief 广播逐笔成交数据（有默认实现的虚函数）
	 * 
	 * 该方法用于广播Level-2行情中的逐笔成交数据。逐笔成交记录了
	 * 市场上每一笔真实成交的详细信息。
	 * 
	 * 逐笔成交数据包含：
	 * - 合约代码、交易所
	 * - 成交时间（微秒级精度）
	 * - 成交价格
	 * - 成交数量
	 * - 成交金额
	 * - 买方委托序号
	 * - 卖方委托序号
	 * - 成交类型（主动买、主动卖、撮合）
	 * 
	 * 默认实现：
	 * - 提供了空实现 {}
	 * - 子类根据需要选择性重写
	 * 
	 * 使用场景：
	 * - 成交量价分析（VWAP等）
	 * - 大单监控和分析
	 * - 市场冲击成本计算
	 * - 资金流向分析
	 * - 买卖力量对比
	 * 
	 * 数据特点：
	 * - 数据量巨大，活跃合约每秒数千笔成交
	 * - 是最重要的Level-2数据之一
	 * - 包含价格和数量的真实成交信息
	 * 
	 * 性能要求：
	 * - 处理速度要快，避免积压
	 * - 建议使用批量处理提高效率
	 * - 可以考虑采样或过滤策略
	 * 
	 * @param curTrans 当前逐笔成交数据的指针
	 * 
	 * @note 虚函数，子类可选择性重写
	 * @note 默认实现为空，不做任何处理
	 * 
	 * @see WTSTransData 逐笔成交数据结构定义
	 */
	virtual void	broadcast(WTSTransData* curTrans){}
};

NS_WTP_END    // 结束WonderTrader命名空间

